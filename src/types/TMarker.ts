export type TMarker = {
    id: string
    name: string
    icon: string
    description: string
    img: string[]
    images: Array<any>
    position: number[]
    color: string
    location: string
}
